'use strict';

// anonymous functions and concise lamba syntax
const y = x => x * 2;

// mutation is a change to data structure that happens in-place
const foo = { bar: 'baz' };

foo.bar = 'qux'; // mutation